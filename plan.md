# Plan

## Current focus
- Refine collapsed code-node declarations so they read as lightweight annotations rather than a floating panel.
- Keep the sidebar above graph content while preserving node-layer interaction.

## Completed
- Collapsed preview now includes both function and class declarations.
- Distinct styling is applied for function vs class entries.
- Sidebar layering was increased relative to graph DOM nodes.

## Next
- Remove box styling and scroll behavior from collapsed declaration preview.
- Keep the declaration list compact and aligned with the node without extra padding or blank spacing.
- Validate the collapsed preview under both function and class declarations.
- Preserve declarations across unrelated node executions and ensure function names display with parentheses.